# cdr_mtn_tv — Captured Prompts

<<<ORIGINAL_PROMPT>>>

OK. Let's start a new project called cdr_mtn_tv. Create directory with a README.md and PLAN.md

This will run on a raspberry pi 4 with 2 hdmi outputs,both running 1080 displays.

Initially we will build several pieces.

1. The first hdmi output (tv1) will display a menu. Format will change but initially a table with a single colum header, a 2 column row with Draft and Food, then 10 multi column headers (will vary but intially name, abv, price, food name, food price) and a final footer row with one column

2. Pulling from tv_display, we will grab that content (copy it into this cdr_mtn_tv folder as events_display) and will maintain everything about it, except migrate the header to a cdr_mtn_tv/assets folder. This will display on a second tv (tv2) in portrait mode

- Both of these displays will be a static picture.

- The first tv will be generated by a web UI that will allow editing of all of the values in a web interface, and then a button will generate a new static image based on that and refresh the display

- the 2nd tv will use the same logic as the current tv_display folder and pull from events onthe website.

- account for a second web interface that will do someactions based on events. This is a TBD that will act on events that have 'streamed' in the title.

-Account for a test/debug mode where we can run this locally and simuilate the dual displays by separate windows on a windows machine which can consist of web pages running on different ports. ie. 3 ports exposed when in test/debug mode, web interface to edit tv1, display tv1, display tv2. Use 9000, 9001, 9002

Create a README.md with this after asking clarifying questions

Create a TODO.md with a plan for implementing this

Ask any clarifyng questions or propopse changes. The idea is to make this as simple as possible. This is a single use application so putting appropriate things in variables/a config file is fine but this is only built for this specific use case.

Capture these instructions in a PROMPTS.md with delimiters

<<<END_ORIGINAL_PROMPT>>>

<<<FOLLOWUP_PROMPT_2025-06-08>>>

1. What other fonts should we consider for our display? This will be for a pub and bakery

2. For web/app.py we need a startup service that will spawn 2 x displays and open the browser on those. And if we are using chronium how do we refresh when things change?

3. Make editor.html and events tab separate pages with a bread crumb at the top linking them

4. I like chromium but I worry about weight and responsiveness. I have seen display delays and artifacts previously. I think debug can display a web page and each display will simply display an aut refreshing image.

3. Flask implementation. Keep simple, no threads.

4. Pi deployment. Create an idempotent script to implement those install steps.
   Cron at 8am only but verify at startup and update as needed in case pi was not powered on at cron time.
   Again questioning chronium

5. README.md use clear delimination for different sections to allow you to do further updates. Ignore tv_display relationship, will be legacy after we are done.

6. PLAN.md looks good

config.json will need to define widths for each column and group, aka group "DRAFT", 60%, "FOOD", 40%, and similar for each menu_column

Make those changes. Ask clarifying questions if unclear.

<<<END_FOLLOWUP_PROMPT>>>

## Decisions Log

| Topic | Decision |
|-------|----------|
| Project location | Inside `canteen_calendar/cdr_mtn_tv/` |
| Plan files | Both `PLAN.md` + `TODO.md` |
| TV1 columns | Config-driven `width_percent` per group and column |
| Pi display | **feh** primary; Chromium optional fallback |
| Debug display | Auto-refreshing `<img>` web pages |
| Flask | No threads; subprocess launcher for debug |
| TV2 refresh | Cron 8 AM + startup refresh on boot |
| Streamed events UI | Separate `/events` page with breadcrumb (not tab) |
| TV1 defaults | Placeholder text in config |
